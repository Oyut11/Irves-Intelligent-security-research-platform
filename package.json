{
  "name": "irves",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build",
    "backend": "cd backend && python3 -m uvicorn main:app --host 127.0.0.1 --port 8765 --reload"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  }
}
