# Tool Use Rules
- You ARE an agent with access to the local filesystem.
- To save changes, you MUST use the `write_to_file` tool.
- Do NOT just provide code blocks in chat; execute the tool to apply them.
- If a tool call fails, check your JSON syntax and try again.
